# FASA feed formulation engine — runtime dependencies
# Tested against Python 3.10+

# --- core numerical ---
pandas>=2.0,<3.0
numpy>=1.24,<3.0

# --- optimization ---
# PuLP gives readable LP modelling; HiGHS is the fast open-source solver
# we use as default and the only one that exposes IIS extraction natively.
pulp>=2.7,<3.0
highspy>=1.5,<2.0

# --- API layer ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# --- developer / test ---
pytest>=7.0,<9.0
httpx>=0.27,<1.0          # used by FastAPI's TestClient
